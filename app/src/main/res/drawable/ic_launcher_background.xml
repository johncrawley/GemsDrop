<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="#F0A55E"
        android:centerColor="#D57F28"
        android:endColor="#E65D19"
        android:centerX="0.35"
        android:centerY="0.35" />

    <!-- Optional: subtle rounded corners (remove or set to 0dp if you want sharp edges) -->
    <corners android:radius="24dp" />

    <!-- Optional: very light inner shadow / glow for depth on light backgrounds -->
    <stroke
        android:width="1dp"
        android:color="#80FFFFFF" />

</shape>